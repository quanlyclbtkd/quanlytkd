# PHASE 4K-6V5U6H5 — Root/Public Parity

Canonical build command: `npm run build:public`

SHA-256 result:

```text
rootFileCount   = 123
publicFileCount = 123
missingPublic   = 0
extraPublic     = 0
hashMismatches  = 0
status          = PASS
```

Runtime scope: `index.html`, `app.js`, `style.css`, `.nojekyll`, `js/**`, `css/**`.

`/public` was regenerated by the canonical build script; it was not patched manually.
