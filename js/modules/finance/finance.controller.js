/**
 * modules/finance/finance.controller.js — Phase 3.3H (Stub)
 * ────────────────────────────────────────────────────────────────
 * Khi hoàn tất tách, file này sẽ chứa:
 *
 *   export function initFinance() { ... }
 *
 * Bao gồm tất cả window.X registrations từ finance.js:
 *   window.skipMonth              — Miễn học phí tháng (báo nghỉ)
 *   window.removeSkip             — Hủy báo nghỉ
 *   window.deleteTx               — Xóa giao dịch
 *   window.quickPay               — Thu học phí nhanh (không form)
 *   window.openQuickPayModal      — Mở modal thu nhanh
 *   window.quickCollectExam       — Thu lệ phí thi
 *   window.processCombo           — Thu combo gia đình (2 học viên)
 *   window.handleQuitOption       — Xử lý nghỉ học (quit + tx đồng thời)
 *   window.saveTx                 — Lưu giao dịch từ form chính
 *   window.formatMonthCompact     — Format chuỗi tháng rút gọn
 *   window.onTxTypeChange         — Xử lý thay đổi loại giao dịch
 *   window.onTxPackageChange      — Cập nhật số tiền theo gói
 *   window.onDescriptionInput     — Autocomplete tên võ sinh
 *   window.onFilterMonthChange    — Đổi tháng → reload data
 *
 * NGUỒN: modules/finance.js lines 88–1250 (approx)
 *
 * STATUS: 🚧 Stub — logic vẫn ở modules/finance.js
 *
 * /// Phase 3.3H — Code Organization
 * ────────────────────────────────────────────────────────────────
 */

export const __stub__ = 'finance.controller — Phase 3.3H stub';
