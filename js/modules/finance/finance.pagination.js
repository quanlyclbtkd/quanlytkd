/**
 * modules/finance/finance.pagination.js — Phase 3.3H (Stub)
 * ────────────────────────────────────────────────────────────────
 * Khi hoàn tất tách, file này sẽ chứa:
 *
 *   export function initTransactionPagination()
 *
 * Bao gồm tất cả transaction pagination logic từ finance.js lines ~1250–1387:
 *   - createPaginationState(PAGE_SIZE)
 *   - _doLoad(cursor, direction)      — query Firestore theo tháng
 *   - _injectControls()               — inject Prev/Next vào DOM
 *   - loadFirstPage()
 *   - window._pgNext_transactions
 *   - window._pgPrev_transactions
 *   - window.reloadTransactionsPage
 *   - _bindMonthReset()               — reset khi đổi tháng
 *   - _bindSearchReset()              — reset khi search thay đổi
 *
 * NGUỒN: modules/finance.js lines 1250–1387
 *
 * STATUS: 🚧 Stub — logic vẫn ở modules/finance.js
 *
 * /// Phase 3.3H — Code Organization
 * ────────────────────────────────────────────────────────────────
 */

export const __stub__ = 'finance.pagination — Phase 3.3H stub';
