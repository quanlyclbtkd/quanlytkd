/**
 * modules/students/students.pagination.js — Phase 3.3H (Stub)
 * ────────────────────────────────────────────────────────────────
 * Khi hoàn tất tách, file này sẽ chứa:
 *
 *   export function initStudentPagination()
 *
 * Bao gồm tất cả pagination logic từ modules/students.js lines ~980–1115:
 *   - createPaginationState(PAGE_SIZE)
 *   - _doLoad(cursor, direction)   — thực thi query Firestore
 *   - _injectControls()            — inject Prev/Next controls vào DOM
 *   - loadFirstPage()              — load trang đầu tiên
 *   - window._pgNext_students      — next page handler
 *   - window._pgPrev_students      — prev page handler
 *   - window.reloadStudentsPage    — reload sau add/edit/delete
 *   - _bindSearchReset()           — debounce search → reset pagination
 *
 * NGUỒN: modules/students.js lines 980–1115
 *
 * STATUS: 🚧 Stub — logic vẫn ở modules/students.js
 *
 * /// Phase 3.3H — Code Organization
 * /// Phase 3.2A — Pagination (already implemented in students.js)
 * ────────────────────────────────────────────────────────────────
 */

export const __stub__ = 'students.pagination — Phase 3.3H stub';
