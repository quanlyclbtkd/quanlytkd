/**
 * modules/students/students.controller.js — Phase 3.3H (Stub)
 * ────────────────────────────────────────────────────────────────
 * Khi hoàn tất tách, file này sẽ chứa:
 *
 *   export function initStudents() { ... }
 *
 * Bao gồm tất cả window.X registrations:
 *   window.openAddModal        — Mở form thêm võ sinh
 *   window.closeAddModal       — Đóng form
 *   window.addNewStudent       — Submit form thêm mới
 *   window.editProfile         — Mở form sửa hồ sơ
 *   window.saveProfile         — Lưu hồ sơ đã sửa
 *   window.deleteStudent       — Xóa võ sinh (có confirm)
 *   window.openProfile         — Mở modal hồ sơ võ sinh
 *   window.filterStudents      — Lọc danh sách theo search
 *   window.openBulkZaloModal   — Mở modal nhắc nợ hàng loạt
 *   window.sendNextZaloMsg     — Gửi Zalo nhắc nợ tuần tự
 *   window.generateQR          — Tạo QR code thanh toán
 *   window.copyAndOpenZalo     — Copy tin nhắn + mở Zalo
 *   window.removeAchievement   — Xóa thành tích khỏi hồ sơ
 *
 * NGUỒN: modules/students.js lines 1–800 (approx)
 *
 * STATUS: 🚧 Stub — logic vẫn ở modules/students.js
 *         Hoàn tất tách sau khi toàn bộ test pass.
 *
 * /// Phase 3.3H — Code Organization
 * ────────────────────────────────────────────────────────────────
 */

// TODO: Khi tách hoàn tất, export initStudents từ đây
// và update modules/students/index.js để import từ file này
// thay vì từ ../students.js

export const __stub__ = 'students.controller — Phase 3.3H stub';
