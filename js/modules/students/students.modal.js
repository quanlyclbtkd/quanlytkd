/**
 * modules/students/students.modal.js — Phase 3.3H (Stub)
 * ────────────────────────────────────────────────────────────────
 * Khi hoàn tất tách, file này sẽ chứa tất cả modal logic:
 *
 *   openAddModal()            — Mở form thêm võ sinh mới
 *   closeAddModal()           — Đóng form thêm
 *   openProfile(name)         — Mở modal xem/sửa hồ sơ
 *   closeProfileModal()       — Đóng modal hồ sơ
 *   populateProfileForm(data) — Điền data vào form sửa hồ sơ
 *   openZaloModal(name,phone) — Mở modal Zalo nhắc nợ
 *   openBulkZaloModal()       — Mở modal nhắc nợ hàng loạt
 *   openQuickPayModal(name)   — Mở modal thu học phí nhanh
 *
 * MODAL PATTERN (Phase 3.3B):
 *   Tất cả modal phải dùng ui/modal.js:
 *     import { openModal, closeModal } from '../../ui/modal.js';
 *
 *   KHÔNG dùng trực tiếp:
 *     document.getElementById('profileModal').style.display = 'flex'; // ❌
 *
 * NGUỒN: modules/students.js — openAddModal, closeAddModal, openProfile
 *         + inline modal HTML injection functions
 *
 * STATUS: 🚧 Stub
 *
 * /// Phase 3.3H — Code Organization
 * ────────────────────────────────────────────────────────────────
 */

export const __stub__ = 'students.modal — Phase 3.3H stub';
