/**
 * modules/students/students.search.js — Phase 3.3H (Stub)
 * ────────────────────────────────────────────────────────────────
 * Khi hoàn tất tách, file này sẽ chứa:
 *
 *   filterStudents(search)    — Lọc danh sách theo từ khóa
 *   filterByBranch(branch)    — Lọc theo cơ sở
 *   filterByBelt(belt)        — Lọc theo đai
 *   filterByStatus(status)    — Lọc theo trạng thái (active/quit)
 *   buildSearchQuery()        — Đọc tất cả filter inputs và build query object
 *   resetSearchFilters()      — Reset tất cả filter về mặc định
 *
 * DEBOUNCE:
 *   Search input nên có debounce 350ms để không trigger
 *   Firestore query khi người dùng đang gõ.
 *
 *   let _debounce = null;
 *   input.addEventListener('input', () => {
 *       clearTimeout(_debounce);
 *       _debounce = setTimeout(() => filterStudents(input.value), 350);
 *   });
 *
 * NGUỒN: modules/students.js window.filterStudents declaration
 *
 * STATUS: 🚧 Stub
 *
 * /// Phase 3.3H — Code Organization
 * ────────────────────────────────────────────────────────────────
 */

export const __stub__ = 'students.search — Phase 3.3H stub';
