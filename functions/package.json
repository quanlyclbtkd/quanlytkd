{
  "name": "taekwondo-cloud-functions",
  "description": "Firebase Cloud Functions — Taekwondo Club Management System",
  "version": "3.0.0",
  "private": true,
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "shell": "firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log",
    "lint": "node -c index.js && node -c src/helpers.js && node -c src/authz.js && node -c src/debtCalculation.js && node -c src/statsAggregation.js && node -c src/superAdminSummary.js"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^4.9.0"
  },
  "devDependencies": {
    "firebase-functions-test": "^3.1.0"
  }
}
