# PHASE 4K-6V5U6H8R2 — PRE-IMPLEMENTATION DEFECT MATRIX

Baseline: H8R1. Static Firestore budget: getDoc <= 29, getDocs <= 51, onSnapshot <= 16.

| ID | Module | File / function | Symptom | Exact root cause | Current canonical authority | Existing reader | Existing writer | Existing listener | Collections | Severity | Reproducible | Planned patch | Read impact | Write impact | Listener impact | Regression risk | Rollback plan |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| R2-A1 | Transactions pagination | `js/modules/finance.js::initTransactionPagination` | Hidden Thu Chi can call pagination `getDocs` after login and hidden month/search changes | Auto-start uses `curTab === 'tx' || document.getElementById('txList')`; `txList` exists while hidden. Month/search handlers always call `_doLoad()` | Existing transaction pagination inside Finance module; realtime authority unchanged | `FinanceService.getTransactionsPage()` | none | Existing canonical transaction listener in app/transaction boundary | transactions | P1 | Yes | Reuse existing `switchTab` activation. Add local active-tab predicate + dirty context state. Hidden month/search only invalidate; actual first-page read only on active tab | Runtime reads decrease; static unchanged | none | none | Medium | Revert finance.js/main.js Patch A only |
| R2-B1 | Attendance daily reads | `js/services/attendance.service.js::loadByDate` | Daily records >=1200 silently incomplete | Single query applies hard `limit(dailyLimit=1200)` and returns partial result | AttendanceService `loadByDate` | one `getDocs` call-site | none | none | attendance | P1 | Yes | Cursor paginate inside same `loadByDate`; same filters; bounded safety ceiling; return metadata; caller stale callback aborts old context | Runtime reads only for actual >page datasets; static unchanged | none | none | High | Revert attendance.service + attendance.js Patch B1 |
| R2-B2 | Attendance bulk writes | `AttendanceService.bulkSaveRecords`; offline `bulkSyncOffline` | >500 writes can exceed Firestore batch limit; partial offline cleanup must remain scoped | One writeBatch over all records | AttendanceService bulk owner | none | existing AttendanceService batch writer | none | attendance | P1 | Yes | Chunk <=400 inside same service writer; throw structured partial result; UI/offline cleanup only committed chunk | none | More commits only when >400 | none | High | Revert AttendanceService Patch B2 |
| R2-C1 | Tuition / transaction form | `TuitionCommandBoundary.collectTuition`, `finance.js transactionForm.onsubmit` | Transaction may exist while profile payment update fails | Primary transaction and profile writes are independent sequential Firestore calls | TuitionCommandBoundary + FinanceService | none | FinanceService | transaction canonical listener | transactions, profiles, fee_audit | P0 | Yes by failure injection | Add atomic prepare/commit primitive to existing FinanceService using one writeBatch; TuitionCommandBoundary and form reuse it; fee_audit remains class-2 after commit | none | Primary writes become one batch | none | High | Revert FinanceService/Tuition boundary/form Patch C |
| R2-C2 | Family combo | `js/modules/finance.js::processCombo` and legacy fallback in app.js | Student1 can commit while student2 fails; tx/profile divergence | Each student's tx and profile patch are sequential, and two students are processed independently | Existing Finance module owner / guarded legacy compatibility | none | FinanceService / legacy owner | canonical transaction listener | transactions, profiles, fee_audit | P0 | Yes | Build one logical plan in memory, commit all primary tx+profile changes in one existing FinanceService batch primitive; audits post-commit | none | One atomic batch | none | High | Revert combo Patch C only |
| R2-C3 | MultiItem | `app.js::processMultiItem` | paidUntil, inventory sale/debt status, stock ledger and bundle tx can diverge | Sequential cross-domain writes before bundle transaction; direct fallback writes | Existing guarded legacy `processMultiItem` owner + InventoryService ledger owner | local RAM only for debt items | app.js guarded writer + InventoryService | canonical transaction listener | profiles, inventory, settings/inventory_stats, transactions | P0 | Yes | Reuse InventoryService pure `prepareAddItemMutation` primitive; guarded existing app.js owner builds one batch for primary writes, deterministic refs, <=400 guard; remove unsafe fallback path for pay | none | One atomic logical batch | none | Very High | Revert app.js + InventoryService C3 patch |
| R2-C4 | Inventory debt payment | `InventoryService.markPaid` | Revenue tx can commit then inventory paid flag fail | Existing tx write/update then inventory update sequential | InventoryService | `getDoc` + `getDocs` existing | InventoryService | transaction listener | inventory, transactions | P0 | Yes | Keep existing reads; pre-resolve existing/new tx then commit transaction + inventory state in one batch | static unchanged | one atomic batch | none | High | Revert markPaid patch |
| R2-D1 | Quit profiles | `profiles.listeners.js::loadQuitProfilesIfNeeded` | Reopening Quit after >60s does a full profiles `getDocs` despite no mutation | `forceRefresh = tabRefreshReason && ageMs > 60000` time-only invalidation | Existing quit authority in profiles.listeners + QuitProfileBoundary | one full `getDocs(ctx.profRef)` | none | active profile listener already signals removals | profiles | P1 | Yes | Remove 60s primary refresh; load only incomplete/dirty/error/club/auth/explicit-force. Existing active-listener removal signal remains dirty source | Runtime reads decrease; static unchanged | none | none | Medium | Revert profiles.listeners Patch D |
| R2-E1 | Exam promotion | `app.js::processBatchUpgrade`, `finishExamSession` | >500 profile writes fail; retry handling not scalable | Single unlimited writeBatch. Promotion itself is guarded by expected current belt before each run | Existing exam guarded legacy owner | RAM profiles | existing batch writer in app.js | profile listener | profiles | P1 | Yes | Per-student-independent classification; chunk <=400. On retry, re-check RAM expected source belt/upgrade marker; only remaining eligible students are written | none | multiple batch commits only >400 | none | High | Revert app.js Patch E |
| R2-F1 | Bootstrap | `index.html::_loadMainWhenLegacyReady` | 50ms polling may continue forever if legacy dependency never becomes ready | Recursive setTimeout has no deadline | Existing bootstrap function | none | none | DOMContentLoaded only | none | P2 | Yes | Same function + 15s deadline, diagnostic + recoverable UI, no auto reload | none | none | none | Low | Revert index Patch F1 |
| R2-F2 | Mobile header | inline `_syncMobileHeader` in `index.html` | Permanent 3s interval for values already covered by MutationObserver/change events | Redundant `setInterval(_syncMobileHeader,3000)` | Existing inline header sync | DOM only | DOM only | MutationObserver + filterMonth change | none | P2 | Yes | Remove interval; add resize and existing app context events using same sync function, idempotent bindings | none | none | no Firestore listener; DOM events only | Low | Revert index Patch F2 |
| R2-F3 | XSS fallback | app.js paths using `window.escapeHtml || (s=>s)` | If canonical escape helper is not loaded yet, user-controlled strings can be inserted raw into HTML | Identity fallback is fail-open | Existing global `window.escapeHtml` from main.js; legacy app renders before/without module possible | none | none | none | none | P0 | Yes | Install one local pure `_safeEscapeHtml` in legacy owner; replace unsafe identity fallbacks; do not create competing global helper | none | none | none | Medium | Revert app.js F3 replacements |

## STOP-condition review

- No patch requires a second Firestore reader/listener authority.
- No schema migration, profile rename, transaction history rewrite, Attendance migration, Rules relaxation, or Cloud Function is required.
- Financial atomicity can be implemented inside the existing FinanceService / TuitionCommandBoundary / guarded `processMultiItem` owner, with an InventoryService *prepare-mutation* primitive explicitly allowed by H8R2. Therefore Patch C is safe to attempt without creating a new authority.
