# PHASE 4K-6V5U6H8R2.1 — PRE-IMPLEMENTATION RESIDUAL DEFECT MATRIX

Baseline: H8R2 source after `check:long-term-production-stability = 27/27 PASS`, static Firestore call sites `29 / 51 / 16`, root/public parity `123/123`.

| ID | Module / File | Function | Symptom | Exact root cause | Current canonical authority | Existing reader | Existing writer | Existing listener | Affected data | Severity | Reproducible | Planned patch | Read impact | Write impact | Listener impact | Regression risk | Rollback plan |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| R1 | MultiItem + Inventory / `app.js`, `js/services/inventory.service.js` | `processMultiItem()`, `InventoryService.markPaid()` | MultiItem can produce `unpaid:false` while legacy `inventoryDebtStatus:'pending'` remains, so the debt classifier can still treat the row as active; debt-only payment also skips existing inventory cache invalidation. | H8R2 atomic MultiItem only wrote `unpaid/paidTxId/paymentBundleId`, while the existing canonical Inventory paid state also requires `inventoryDebtStatus:'paid'`, `paidAt`, `paidDate`. Cache notification was nested under `_preparedInventory`, excluding debt-only payments. | Existing MultiItem one logical `writeBatch`; InventoryService ledger owner | none added | same MultiItem batch | canonical inventory listeners unchanged | `inventory`, runtime inventory history/cache | P1 | Yes | Add PURE `InventoryService.prepareMarkPaidPatch()` and reuse returned patch in same batch; call existing `notifyInventoryMutation(...,{writeThrough:true})` when `_preparedInventory || invDebtIds.length>0`. | 0 | same batch operation count | 0 | Medium | Remove helper usage + restore prior inline patch/notification condition. |
| R2 | MultiItem tuition / `app.js` | `processMultiItem()` | `paidUntil` can move backwards (e.g. `2026-12 -> 2026-10`). Audit payload can report the wrong final boundary. | H8R2 writes raw `lastMonth` without comparing normalized existing `profile.paidUntil`. | Existing MultiItem atomic writer + Tuition month semantics | none added | same profile update in same batch | none | `profiles.paidUntil`, debt coverage, financial audit | P0 | Yes | Reuse existing `normalizeYYYYMM`; compute `previous/candidate/resultPaidUntil = max(existing,candidate)`, write result and audit the same result. | 0 | same one profile update | 0 | High | Revert only paidUntil calculation if tuition gates regress. |
| R3 | MultiItem profile identity / `app.js` | `processMultiItem()` | Raw name not found in canonical RAM can still create a ghost profile through `batch.set(...,{merge:true})`. | `const profile = allProfiles[name] || {}` silently converts missing canonical identity into an empty object, then profile tuition write uses create-if-missing `set`. | Existing profile canonical map + same MultiItem atomic writer | none added | same batch | none | `profiles`, financial transaction/debt links | P0 | Yes | Require `allProfiles[name]` before write-plan construction; fail visibly with 0 writes; use `batch.update(profileRef, ...)` so unexpected deletion aborts the whole batch. | 0 | safer existing batch write | 0 | High | Restore prior profile operation only if source gate proves an incompatible write abstraction. |
| R4 | Quit profile cache / `js/listeners/profiles.listeners.js`, `js/core/studentStatusCommandBoundary.js`, `js/main.js` | canonical profile mutation commit + quit authority | Editing a profile already in `quit` state can leave the complete Quit cache stale indefinitely because active-query membership does not change. | H8R2 invalidation relies mainly on active listener add/remove; same-status quit-profile field edits are invisible to that membership transition signal after TTL removal. | Existing StudentStatusCommandBoundary + existing Quit authority in profiles listener | existing lazy full snapshot only | existing StudentService writer only | existing active profile listener only | quit profile display fields/cache | P1 | Yes | Export a PURE/local `markQuitAuthorityDirty(reason)` from existing profiles listener; expose through existing main bridge; call after successful canonical mutation only when previous or next profile status is quit. | 0 | 0 extra | 0 | Medium | Remove local dirty call/export; active↔quit membership invalidation remains as H8R2 baseline. |
| R5 | Attendance / `js/services/attendance.service.js` | `AttendanceService.loadByDate()` | Missing `limit` can run unbounded; missing `startAfter` can stop after page 1 and return partial coverage as if complete. | H8R2 treats pagination primitives as optional (`warn`, conditional cursor) instead of a required capability of the canonical paged reader. | Existing `loadByDate()` daily read authority | same `getDocs` call site | none | none | daily attendance coverage | P1 | Yes | At function entry require both `limit` and `startAfter`; otherwise throw code `attendance/daily-pagination-unavailable` before query. | 0 call-site change; failure occurs before read | 0 | 0 | Low | Remove primitive guard if SDK contract unexpectedly differs; do not restore partial-success behavior. |

## Stop-condition audit

No residual requires a second reader/writer/listener, Cloud Function, Rules relaxation, migration, profile rename, Attendance schema rewrite, transaction mass rewrite, or additional Firestore call site. All five defects can be patched inside the current H8R2 owners.
