/**
 * modules/finance/finance.receipt.js — Phase 3.3H (Stub)
 * ────────────────────────────────────────────────────────────────
 * Khi hoàn tất tách, file này sẽ chứa:
 *
 *   generateReceipt(txData)       — Tạo biên lai học phí (print/PDF)
 *   generateMultiMonthPaymentRequest(name, months, branch, total)
 *                                 — Tạo QR + thông tin thanh toán đa tháng
 *   printReceipt(txId)            — In biên lai
 *   generateQRPaymentData(data)   — Build VietQR data string
 *
 * CANVAS/LOGO:
 *   Biên lai dùng canvas để vẽ logo CLB.
 *   Đọc logoCanvasData từ window.__store.logoCanvasData.
 *
 * NGUỒN: app.js receipt + QR section
 *
 * STATUS: 🚧 Stub — logic vẫn trong app.js
 *
 * /// Phase 3.3H — Code Organization
 * ────────────────────────────────────────────────────────────────
 */

export const __stub__ = 'finance.receipt — Phase 3.3H stub';
