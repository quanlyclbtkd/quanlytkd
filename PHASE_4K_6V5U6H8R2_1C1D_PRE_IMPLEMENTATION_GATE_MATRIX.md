# PHASE 4K-6V5U6H8R2.1C1D — PRE-IMPLEMENTATION GATE MATRIX

## Baseline captured before any patch

- Mobile UI Shell: PASS 80/80
- Syntax: PASS 246/246
- Canonical Transaction: PASS 27/27
- Financial Action Audit Guard: PASS
- Runtime Stability: PASS 29/29
- Long-Term Production Stability: PASS 39/39
- Critical Precheck: PASS 41/41
- Release Gate: PASS 36/36
- Deploy Package: PASS 12/12
- Root/Public parity: PASS 124/124
- Production Residual Defect Closure: PASS 66/66
- Firestore static budget from critical precheck: getDoc=29, getDocs=51, onSnapshot=16

Production source hashes were captured in `_c1d_evidence/baseline/production_hashes.sha256` before edits.

## Defect matrix

| ID | gate | current failure | affected module | exact assertion | expected old behavior | current canonical behavior | source evidence | test evidence | source defect? | stale test? | production impact | planned action | files allowed to change | Firestore read impact | Firestore write impact | listener impact | regression risk |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| G1A | `check:multiitem-tuition-package-fix` | `paidUntil lastMonth missing` | Multi-item tuition payment | literal `app.includes("paidUntil: lastMonth")` | profile update writes package `lastMonth` directly | H8R2.1 computes normalized `previousPaidUntil`, `candidatePaidUntil`, then `_resultPaidUntil=max(previous,candidate)` and writes that result | `app.js` processMultiItem contains `_previousPaidUntil`, `_candidatePaidUntil`, `_resultPaidUntil`; batch profile update uses `paidUntil: _resultPaidUntil`; canonical bundle is still committed | Gate fails only on removed literal while all financial/canonical baseline gates pass | **NO** | **YES** | Current source prevents a back-payment from moving paidUntil backwards; restoring old literal would regress debt correctness | Replace literal assertion with semantic extraction/execution of the production monotonic block; assert T1–T6 | `tools/check-multiitem-tuition-package-fix.mjs` | 0 | 0 | 0 | Low |
| G1B | `check:inventory-multiitem-readonly-ui` | `processMultiItem still writes paidUntil when tuition paid` | Multi-item read-only UI ownership gate | regex `/paidUntil:\s*lastMonth/` inside processMultiItem | same old literal boundary | same monotonic `_resultPaidUntil` production behavior; read-only UI module remains write-free | `app.js` processMultiItem writes `_resultPaidUntil`; `inventoryMultiItemReadOnlyUI.js` has no Firestore writes | Gate's ownership assertions pass; only paidUntil literal assertion fails | **NO** | **YES** | False negative only; production source is safer than stale expectation | Reuse semantic monotonic evidence and assert profile batch update uses computed result, not local variable name formatting | `tools/check-inventory-multiitem-readonly-ui.mjs` | 0 | 0 | 0 | Low |
| G2 | `check:global-ownership-adoption-cleanup` | runtime mock throws `TypeError: sheet.setAttribute is not a function` | Test DOM fixture vs canonical mobile owner | mock element lacks DOM attributes API | old shell did not require ARIA mutation | canonical `legacyUiShell.js` sets `aria-hidden` and trigger `aria-expanded`, plus body scroll lock | `js/ui/legacyUiShell.js` calls `setAttribute` in open/close; Mobile UI Shell 80/80 already validates canonical accessibility contract | Failure stack originates in test fixture at `openMobileMenu`, not source behavior | **NO** | **YES** | None in production; removing ARIA would be an accessibility regression | Extend fixture with minimal `setAttribute/getAttribute/removeAttribute`, event methods, hidden/query APIs actually used; add open/close ARIA + scroll/idempotence assertions | `tools/check-global-ownership-adoption-cleanup.mjs` | 0 | 0 | 0 | Low |
| G3 | `check:inventory-dynamic-size-catalog` | `New inventory items maintain inventory_stats using increments` | Inventory canonical service | source string requires `batch.set(statsRef, summaryPatch, ...)` | direct local `summaryPatch` consumed by writer | current prepare→commit path returns `{statsRef, summaryPatch}` and commit consumes `prepared.summaryPatch` in same canonical writer | `inventory.service.js`: `prepareAddItemMutation()` builds `_buildLedgerIncrementPatch`; `addItem()` calls `batch.set(prepared.statsRef, prepared.summaryPatch,{merge:true})`; no getDocs in add path | 27/28 pass; only variable-shape string assertion fails | **NO** | **YES** | None; changing source back to variable-name shape would be cosmetic and risky | Replace local-name literal with semantic prepare→consume assertions, increment-patch provenance, same batch boundary, no extra read/write owner | `tools/check-inventory-dynamic-size-catalog.mjs` | 0 | 0 | 0 | Low |
| G4 | `check:v5r-quit-single-source-lock` | freshness revalidation assertion fails (3 dependent checks total result 13/16) | Profiles/Quit authority | requires `quitAuthorityLoadedAt`, `ageMs > 60000`, `forceRefresh` | 60s TTL forces full getDocs refresh | H8R2 D: completeness + mutation dirty state is primary invalidation; stable complete cache returns without read; dirty/error/explicit-force may refresh | `profiles.listeners.js` explicitly states waiting 60s MUST NOT trigger full getDocs; `markQuitAuthorityDirty()` sets dirty/completeness false; current quit tab calls `ensureQuitProfilesComplete()` on membership removal | Long-Term Stability check #37 passes event-driven quit reuse/one refresh; stale gate still requires removed TTL semantics | **NO** | **YES** | Restoring timer would increase reads and violate phase architecture | Remove TTL requirement; assert forbidden 60s/timer polling, dirty mutation signal, active-tab authoritative refresh, valid-cache no-refresh contract, single existing reader, Coach fail-closed | `tools/check-v5r-quit-single-source-lock.mjs` | 0 | 0 | 0 | Low |
| G5 | `check:v5t-command-boundary-write-freeze` | fee_audit loop reported as new direct-write signature | V5T write-freeze normalizer | signature multiset compares one loop row `{..._row}` against two pre-loop unrolled historical signatures | two separate fee_audit call lines for n1/n2 | current canonical primary combo commit occurs first; then existing Class-2 fee_audit projection loops rows, catches failure and records secondary consistency failure | `app.js`: `commitAtomicWritePlan(...)` precedes `for (const _row of _auditRows) addDoc(...fee_audit...)`; catch calls `_recordSecondaryConsistencyFailure(...canonicalPaymentPreserved:true)`; baseline already sanctions two fee_audit writes | total/per-op write counts remain within frozen baseline; only signature normalizer rejects reshaped existing projection | **NO** | **YES** | False positive can block release; deleting fee_audit would reduce observability | Update normalizer to recognize bounded approved fee_audit projection loop and map it to existing baseline capacity without increasing allowed total/per-op counts; assert class-2 diagnostics/no blind retry | `tools/check-v5t-canonical-command-boundary-write-freeze.mjs` and, only if necessary, baseline normalizer metadata (not threshold) | 0 | **0 new authority** | 0 | Medium-low |

## Audit conclusion before implementation

All six observed failures are classified **STALE TEST / STALE FIXTURE / STALE NORMALIZER**, not proven production-source defects. Therefore production source (`app.js`, `index.html`, `js/**`, `css/**`, `firestore.rules`) remains READ-ONLY for gate realignment. Any later production-source change requires a separately reproducible runtime defect and explicit evidence.
